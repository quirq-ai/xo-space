# HTTP routes

Generated by `scripts/write_route_docs.py` from the mounted routers; do not edit by hand.
Every route below belongs to a module (`modules/<name>/routes.py`), mounted behind that
module's `api` switch: a module turned off in Setup answers 404 `module_disabled`. Streams
(`/api/<module>/stream/<name>`) are server-sent events. The kernel's own routes are listed last;
the active agent's own routes (`adapters/<name>/routes.py`) are not listed, they depend on AGENT_NAME.

## Agent (`modules/agent`)

The agent side: chat, sessions, files, connectors, auth and the broker routes, plus the GitHub issue poller and the one-shot boot steps.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/agents` | Return the sidebar agents for the active backend (``AGENT_NAME`` env). |
| POST | `/api/agents` |  |
| DELETE | `/api/agents/{agent_id}` | Delete an agent, resolved to whichever backend owns the id. Backends |
| GET | `/api/agents/{agent_id}` |  |
| PATCH | `/api/agents/{agent_id}` |  |
| GET | `/api/automations` |  |
| GET | `/api/channels` | Return the list of messaging channels with their connection state. |
| POST | `/api/channels/add` | Onboarding + Settings → Channels target. |
| GET | `/api/chat/active` |  |
| GET | `/api/config/agents/{name}` | Unified per-agent config loader the FE Settings → Config tab calls. |
| GET | `/api/config/api-key` |  |
| GET | `/api/config/local` |  |
| GET | `/api/config/ollama` |  |
| GET | `/api/config/openai-subscription` |  |
| GET | `/api/config/openyak-account` |  |
| GET | `/api/config/providers` |  |
| DELETE | `/api/config/providers/{provider_id}/key` | Remove a settings-env provider's keys from the agent's config file. |
| POST | `/api/config/providers/{provider_id}/key` | Persist a provider API key and kick off the agent's CLI chain. |
| GET | `/api/config/workspace` | Return workspace root paths for each agent backend. |
| GET | `/api/connectors` |  |
| POST | `/api/files/content` | Read text content of a file. |
| POST | `/api/files/content-binary` | Read binary file and return as a downloadable response. |
| POST | `/api/files/list-directory` | List files and directories at a given path. |
| POST | `/api/files/mkdir` | Create a new directory under the user's home directory. |
| POST | `/api/files/save` | Write text content to a file under the user's home directory. |
| POST | `/api/files/upload` | Save an uploaded file into the workspace (or ~/uploads fallback). |
| GET | `/api/fts/index/{workspace}` |  |
| POST | `/api/fts/index/{workspace}` |  |
| GET | `/api/inbox` |  |
| PATCH | `/api/inbox` |  |
| POST | `/api/inbox` |  |
| DELETE | `/api/inbox/{item_id}` |  |
| PATCH | `/api/inbox/{item_id}` |  |
| GET | `/api/mcp/status` |  |
| GET | `/api/models` | Return one model row per agent/profile under the active backend. |
| GET | `/api/ollama/status` |  |
| GET | `/api/plugins/status` |  |
| GET | `/api/quirq` |  |
| GET | `/api/skills` |  |
| GET | `/api/skills/catalog` |  |
| POST | `/api/skills/install` |  |
| GET | `/api/tools` | Aggregate tools across the caller's connected Composio toolkits. |
| DELETE | `/api/workspace-memory` |  |
| GET | `/api/workspace-memory` |  |
| PUT | `/api/workspace-memory` |  |
| POST | `/api/workspace-memory/export` |  |
| GET | `/api/workspace-memory/list` |  |
| POST | `/api/workspace-memory/refresh` |  |
| POST | `/api/xo-projects-sync/all` |  |
| POST | `/api/xo-projects-sync/all/restore` |  |
| GET | `/api/xo-projects-sync/projects` |  |
| POST | `/api/xo-projects-sync/projects/{project_id}` |  |
| POST | `/api/xo-projects-sync/projects/{project_id}/restore` |  |
| POST | `/api/xo-projects-sync/setup` | Bootstrap config. Does NOT create any repos — those are created lazily on first backup. |
| GET | `/api/xo-projects-sync/status` | Lightweight: report current config + token source. No GitHub network calls. |
| GET | `/api/xo-projects/activity` | Workspace-wide live presence: the union of every project's open sessions. |
| GET | `/channels/status` | Return per-agent channel status. Dispatches on AGENT_NAME. |
| DELETE | `/connect/claude-code` | Explicitly cancel the active setup-token session, if any (the D6 escape |
| POST | `/connect/claude-code` | Run ``claude auth login --claudeai`` and stream stdout/stderr via SSE. |
| POST | `/connect/claude-code/callback` | Send the pasted OAuth code to the running ``claude auth login`` process. |
| POST | `/connect/codex` | Run `codex login --device-auth` and stream its output via SSE. |
| GET | `/models/status` | Return per-agent model-centric status. Dispatches on AGENT_NAME. |
| GET | `/openclaw/usage/analytics` | The active agent's time-series dashboard payload. |
| GET | `/openclaw/usage/sessions` | List every discovered session with basic metadata. |
| GET | `/openclaw/usage/sessions/{session_id}` | Detailed SessionCostSummary for one session, optionally windowed. |
| GET | `/openclaw/usage/summary` | The active agent's SessionCostSummary across every session in the window. |
| GET | `/openclaw/usage/summary/card` | Lightweight headline card: totals plus per-day cost, tokens and assistant messages. |
| GET | `/providers/status` | Return per-agent provider connection status. |
| POST | `/xo-auth/consume` | Consume auth flow and store token in-memory for outgoing XO backend calls. |
| POST | `/xo-auth/logout` | Clear stored auth token state. |
| POST | `/xo-auth/start` | Start XO backend browser auth flow. |
| GET | `/xo-auth/state` | Get current auth state (safe view). |
| GET | `/xo-auth/status/{auth_session_id}` | Poll XO backend auth flow status. |
| GET | `/xo-auth/whoami` | Validate stored token against XO backend /get-user-id endpoint. |

## Connections (`modules/connections`)

What your connected accounts collected, polled on a schedule: one folder per toolkit with its config, cursors and events.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/connections` |  |
| DELETE | `/api/connections/{toolkit}` |  |
| GET | `/api/connections/{toolkit}` |  |
| PUT | `/api/connections/{toolkit}` |  |
| POST | `/api/connections/{toolkit}/account` |  |
| GET | `/api/connections/{toolkit}/events` |  |
| POST | `/api/connections/{toolkit}/poll` |  |
| GET | `/api/connections/stream/events` | server-sent events |

## Connectors (`modules/connectors`)

Connected accounts and tools: Composio toolkits, Google Drive, OneDrive, GitHub, Vercel, and the MCP proxy agents use.

| Method | Path | What it does |
|---|---|---|
| GET | `/.well-known/oauth-protected-resource` | Tell MCP clients which authorization server issues tokens for us. |
| DELETE | `/api/connectors/composio/api-key` |  |
| PUT | `/api/connectors/composio/api-key` |  |
| GET | `/api/connectors/composio/backend` |  |
| GET | `/api/connectors/composio/callback` |  |
| GET | `/api/connectors/composio/toolkits` |  |
| GET | `/api/connectors/composio/{toolkit}/accounts` | Every connected account the XO account holds for one toolkit. |
| PUT | `/api/connectors/composio/{toolkit}/accounts/{connected_account_id}/alias` | Label one connected account, or clear its label with a null/empty alias. |
| POST | `/api/connectors/composio/{toolkit}/accounts/{connected_account_id}/unlink` | Stop using one connected account *in this workspace*. Nothing is deleted. |
| POST | `/api/connectors/composio/{toolkit}/connect` |  |
| POST | `/api/connectors/composio/{toolkit}/disconnect` | Delete a connected account. **Account-wide**: every workspace loses it. |
| GET | `/api/connectors/composio/{toolkit}/prefs` |  |
| PUT | `/api/connectors/composio/{toolkit}/prefs` |  |
| GET | `/api/connectors/composio/{toolkit}/scope` |  |
| PUT | `/api/connectors/composio/{toolkit}/scope` | Choose what this workspace reaches for one toolkit. |
| GET | `/api/connectors/composio/{toolkit}/status` |  |
| GET | `/api/connectors/composio/{toolkit}/tools` |  |
| GET | `/api/connectors/gdrive/remotes` |  |
| POST | `/api/connectors/gdrive/remotes` |  |
| DELETE | `/api/connectors/gdrive/remotes/{name}` |  |
| GET | `/api/connectors/gdrive/remotes/{name}/folders` |  |
| POST | `/api/connectors/gdrive/remotes/{name}/mkdir` |  |
| POST | `/api/connectors/gdrive/remotes/{name}/rmdir` |  |
| POST | `/api/connectors/gdrive/remotes/{name}/upload` |  |
| GET | `/api/connectors/gdrive/sessions/{session_id}` |  |
| POST | `/api/connectors/gdrive/sessions/{session_id}/cancel` |  |
| POST | `/api/connectors/gdrive/sessions/{session_id}/submit` | Receive the redirect URL / verification code the user pasted from the browser. |
| POST | `/api/connectors/github/cli/cancel` | Abort an in-progress CLI login. |
| POST | `/api/connectors/github/cli/poll` | Check the status of an in-progress CLI login. |
| POST | `/api/connectors/github/cli/start` | Spawn `gh auth login --web` and return the device code + verification URL. |
| POST | `/api/connectors/github/disconnect` | Delete the stored GitHub token and clear the connection. |
| POST | `/api/connectors/github/reconnect` | Re-validate the stored token and return the new status. |
| GET | `/api/connectors/github/status` | Return the current GitHub connector status. |
| POST | `/api/connectors/github/token` | Validate a GitHub PAT, store it, and return the connection status. |
| POST | `/api/connectors/magicpath/login` | Login bootstrap + code exchange. |
| POST | `/api/connectors/magicpath/logout` | Log out (idempotent — already-logged-out still returns 200). |
| POST | `/api/connectors/magicpath/setup` | Install the MagicPath skill + CLI. Idempotent; one run at a time. |
| GET | `/api/connectors/magicpath/status` | Install + auth snapshot. Never errors: probe failures read as false. |
| GET | `/api/connectors/onedrive/remotes` |  |
| POST | `/api/connectors/onedrive/remotes` |  |
| DELETE | `/api/connectors/onedrive/remotes/{name}` |  |
| GET | `/api/connectors/onedrive/sessions/{session_id}` |  |
| POST | `/api/connectors/onedrive/sessions/{session_id}/cancel` |  |
| POST | `/api/connectors/onedrive/sessions/{session_id}/submit` | Receive the redirect URL / verification code the user pasted from the browser. |
| POST | `/api/connectors/vercel/disconnect` |  |
| POST | `/api/connectors/vercel/oauth/exchange` | Finish an authorization without a working redirect. |
| GET | `/api/connectors/vercel/oauth/start` | Start an authorization. |
| POST | `/api/connectors/vercel/reconnect` | Re-check the stored credentials, refreshing an OAuth token if due. |
| GET | `/api/connectors/vercel/status` |  |
| POST | `/api/connectors/vercel/token` |  |
| GET | `/callback` | Where Vercel sends the browser after the user approves or denies. |
| DELETE | `/mcp/composio-proxy` |  |
| GET | `/mcp/composio-proxy` |  |
| POST | `/mcp/composio-proxy` |  |
| DELETE | `/mcp/composio-proxy/` |  |
| GET | `/mcp/composio-proxy/` |  |
| POST | `/mcp/composio-proxy/` |  |
| DELETE | `/mcp/composio-proxy/u/{token}` |  |
| GET | `/mcp/composio-proxy/u/{token}` |  |
| POST | `/mcp/composio-proxy/u/{token}` |  |
| DELETE | `/mcp/composio-proxy/u/{token}/` |  |
| GET | `/mcp/composio-proxy/u/{token}/` |  |
| POST | `/mcp/composio-proxy/u/{token}/` |  |
| DELETE | `/mcp/cowork-proxy` |  |
| GET | `/mcp/cowork-proxy` |  |
| POST | `/mcp/cowork-proxy` |  |
| DELETE | `/mcp/cowork-proxy/` |  |
| GET | `/mcp/cowork-proxy/` |  |
| POST | `/mcp/cowork-proxy/` |  |
| DELETE | `/mcp/cowork-proxy/u/{token}` |  |
| GET | `/mcp/cowork-proxy/u/{token}` |  |
| POST | `/mcp/cowork-proxy/u/{token}` |  |
| DELETE | `/mcp/cowork-proxy/u/{token}/` |  |
| GET | `/mcp/cowork-proxy/u/{token}/` |  |
| POST | `/mcp/cowork-proxy/u/{token}/` |  |

## Jobs (`modules/jobs`)

Saved commands that run on a schedule or on demand, with their run history.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/schedules` |  |
| POST | `/api/schedules` |  |
| DELETE | `/api/schedules/{job_id}` |  |
| GET | `/api/schedules/{job_id}` |  |
| PUT | `/api/schedules/{job_id}` |  |
| POST | `/api/schedules/{job_id}/run` |  |
| GET | `/api/schedules/{job_id}/runs` |  |

## Projects (`modules/projects`)

Every project in the workspace: its records (.xo/), its runtime folder, its GitHub mirror, and the graphs of the workspace.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/workspace/workitems` | Every workitem in the workspace, filtered on the **projected** view. |
| GET | `/api/xo-projects` | Every project under the projects root, newest first; never a path. |
| POST | `/api/xo-projects` | Clone a repository into the projects root as a new project. |
| GET | `/api/xo-projects/timeline` | Multiplexed workspace timeline: every project's log plus the Space log, |
| DELETE | `/api/xo-projects/{project_id}` | Remove a local project folder after fresh sharing checks. |
| GET | `/api/xo-projects/{project_id}/activity` | Live presence: which sessions are open in this project right now. |
| GET | `/api/xo-projects/{project_id}/file` | Return one previewable text file's content. |
| GET | `/api/xo-projects/{project_id}/file-history` | Return the git log of one project file's edits. |
| GET | `/api/xo-projects/{project_id}/github/issues` | The GitHub issue mirror for this project, and what is untracked. |
| POST | `/api/xo-projects/{project_id}/github/issues/{issue_number}/adopt` | Track a GitHub issue as a workitem (§7.2, D2). |
| GET | `/api/xo-projects/{project_id}/peers` | Everyone this project is shared with, oldest first. |
| POST | `/api/xo-projects/{project_id}/peers` | Add a collaborator to the roster. |
| DELETE | `/api/xo-projects/{project_id}/peers/{user_id}` | Remove a collaborator. **A hard delete: there is no tombstone.** |
| GET | `/api/xo-projects/{project_id}/peers/{user_id}` | Fetch one peer by ``user_id``. |
| PATCH | `/api/xo-projects/{project_id}/peers/{user_id}` | Change a peer's ``role``, ``label`` or ``endpoint``. |
| GET | `/api/xo-projects/{project_id}/removal` | Whether the local project can be removed, and what blocks it. |
| GET | `/api/xo-projects/{project_id}/timeline` | Newest-first event stream for one project (its log, through ``modules.timeline``). |
| GET | `/api/xo-projects/{project_id}/todos` | Per-session task list for one project. |
| POST | `/api/xo-projects/{project_id}/todos` | Create a new todo under the project (any runtime can call). |
| DELETE | `/api/xo-projects/{project_id}/todos/{todo_id}` | Soft delete: the record is tombstoned (``deleted_at`` set), never removed, |
| GET | `/api/xo-projects/{project_id}/todos/{todo_id}` | Fetch one todo by id. |
| PATCH | `/api/xo-projects/{project_id}/todos/{todo_id}` | Update fields on an existing todo. Most common: status transition |
| GET | `/api/xo-projects/{project_id}/tree` | One level of a project's folder, dotfiles and editor leftovers hidden. |
| GET | `/api/xo-projects/{project_id}/workitems` | Every workitem in the project, oldest first. |
| POST | `/api/xo-projects/{project_id}/workitems` | Create a workitem under the project (any runtime can call). |
| DELETE | `/api/xo-projects/{project_id}/workitems/{workitem_id}` | Soft delete: the record is tombstoned (``deleted_at`` set), never removed, |
| GET | `/api/xo-projects/{project_id}/workitems/{workitem_id}` | Fetch one workitem by id. |
| PATCH | `/api/xo-projects/{project_id}/workitems/{workitem_id}` | Update fields on an existing workitem. |
| DELETE | `/api/xo-projects/{project_id}/workitems/{workitem_id}/adoption` | Stop mirroring the issue. **Keep the workitem** (§7.2). |
| PUT | `/api/xo-projects/{project_id}/workitems/{workitem_id}/assignee` | Set (or clear) who owes this workitem. **Always a local write.** |
| DELETE | `/api/xo-projects/{project_id}/workitems/{workitem_id}/claim` | Drop the claim: the explicit half of "stopped working on this". |
| POST | `/api/xo-projects/{project_id}/workitems/{workitem_id}/claim` | Record that a session is working this workitem. |
| GET | `/xo/dashboard.json` | The graph collapsed into five purpose environments. Same schema as |
| GET | `/xo/space.json` | The workspace graph: projects, folders, files, ties, git history. |

## Sessions (`modules/sessions`)

Every agent session the Space knows: its index row, its purpose, its transcript, and the chat that drives it.

| Method | Path | What it does |
|---|---|---|
| POST | `/api/chat/abort` | Forget a registered turn that has not been played ({stream_id}). |
| POST | `/api/chat/prompt` | Register a turn: {text, session_id?, agent_name?, agent_id?, workspace?, agent_type?, model?} |
| POST | `/api/chat/respond` | Acknowledge a permission response (no-op). |
| GET | `/api/chat/stream/{stream_id}` | Play a registered turn as server-sent events (text-delta, session-created, done, ...). |
| GET | `/api/messages/{session_id}` | The full record of a session: every message with its parts. |
| GET | `/api/sessions` | The sessions the active backend knows, newest first, one page. |
| POST | `/api/sessions` | A fresh session id; the session itself starts with the first prompt. |
| GET | `/api/sessions/search` | Sessions whose title contains q, as {session, snippet} rows. |
| DELETE | `/api/sessions/{session_id}` | Acknowledge a delete; the agent's store keeps the record. |
| GET | `/api/sessions/{session_id}` | One session by id. |
| PATCH | `/api/sessions/{session_id}` | Set the session's working directory ({directory}). |
| GET | `/api/sessions/{session_id}/files` | The session's files (always empty here). |
| GET | `/api/sessions/{session_id}/todos` | The session's todos (always empty here; the projects module holds them). |
| GET | `/api/sessions/{session_id}/transcript` | {title, messages:[{id, role, content}]}: one text bubble per turn. |
| GET | `/api/sessions/stream/events` | server-sent events |

## Settings (`modules/settings`)

Space-wide choices: the runtime settings and roots, secrets, onboarding, and which modules run.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/onboarding` | Whether the first-run flow was completed, and when. |
| POST | `/api/onboarding/complete` | Mark the first-run flow completed, now. |
| GET | `/api/runtime-config` | The Setup status: configured and applied settings, restart state, roots, agents, paths. |
| PUT | `/api/runtime-config` | Save the runtime settings to runtime.env; they apply on the next restart. |
| POST | `/api/runtime-config/restart` | Compatibility alias for the Setup process control. |
| PUT | `/api/runtime-config/roots` | Save the projects root and the state root to roots.env; they apply on the next restart. |
| GET | `/api/secrets` | List the saved secrets with fixed masks; values never cross the wire. |
| PUT | `/api/secrets` | Replace the whole store; the masked listing after. |
| GET | `/api/secrets/env` | Return the active secrets store as a list of key-value entries. |
| PUT | `/api/secrets/env` | Overwrite the active secrets store with the provided entries. |
| GET | `/api/secrets/env/keys` | Return only the keys with non-empty values; no secret material is transmitted. |
| DELETE | `/api/secrets/{key}` | Delete one key; idempotent. |
| PATCH | `/api/secrets/{key}` | Set or update one key. |
| GET | `/api/secrets/{key}/reveal` | The raw value of one key. |

## Sharing (`modules/sharing`)

Projects shared with you and by you: the commit relay that keeps them in step.

| Method | Path | What it does |
|---|---|---|
| POST | `/api/project-sharing/check` |  |
| GET | `/api/project-sharing/status` |  |
| POST | `/api/xo-projects/{project_id}/apply` |  |
| GET | `/api/xo-projects/{project_id}/commits` |  |
| GET | `/api/xo-projects/{project_id}/members` |  |
| POST | `/api/xo-projects/{project_id}/revoke` |  |
| POST | `/api/xo-projects/{project_id}/share` |  |
| GET | `/api/sharing/stream/events` | server-sent events |

## Telemetry (`modules/telemetry`)

What the agents spent and did: usage, stats, session telemetry and the watcher that ingests them.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/telemetry/sources` | Every telemetry provider with its effective data path and switch. |
| PUT | `/api/telemetry/sources/{source_id}` | Save a source's path (empty clears the override) and/or flip its collection. |
| GET | `/api/usage` | Aggregated UsageStats for the active agent. |
| GET | `/api/usage/analytics` | The active agent's time-series dashboard payload. |
| GET | `/api/usage/sessions` | List every discovered session with basic metadata. |
| GET | `/api/usage/sessions/{session_id}` | Detailed SessionCostSummary for one session, optionally windowed. |
| GET | `/api/usage/summary` | The active agent's SessionCostSummary across every session in the window. |
| GET | `/api/usage/summary/card` | Lightweight headline card: totals plus per-day cost, tokens and assistant messages. |
| GET | `/api/xo-projects/usage` | Workspace-aggregated ``UsageStats`` (the ``/api/usage`` shape). |
| GET | `/api/xo-projects/usage/analytics` | Workspace-wide analytics dashboard, the ``/api/usage/analytics`` shape. |
| GET | `/api/xo-projects/usage/sessions` | Every session across every project, each row tagged with its projectId. |
| GET | `/api/xo-projects/usage/sessions/{session_id}` | One session anywhere in the workspace, by composite key or nativeSessionId. |
| GET | `/api/xo-projects/usage/summary` | The aggregate SessionCostSummary over every project. |
| GET | `/api/xo-projects/usage/summary/card` | Workspace-wide usage card (all projects combined). |
| GET | `/api/xo-projects/{project_id}/usage/analytics` | Per-project analytics dashboard, the ``/api/usage/analytics`` shape. |
| GET | `/api/xo-projects/{project_id}/usage/sessions` | List sessions for one project, the ``/api/usage/sessions`` shape. |
| GET | `/api/xo-projects/{project_id}/usage/sessions/{session_id}` | One session of one project, by composite key or nativeSessionId. |
| GET | `/api/xo-projects/{project_id}/usage/summary` | The aggregate SessionCostSummary across one project's sessions. |
| GET | `/api/xo-projects/{project_id}/usage/summary/card` | Lightweight usage widget for one project. |
| GET | `/xo/sessions.json` | Session telemetry merged across every runtime that reports it. |

## Timeline (`modules/timeline`)

What happened, per project and across the Space: one log per project, written once, read merged.

| Method | Path | What it does |
|---|---|---|
| GET | `/api/timeline` |  |
| GET | `/api/timeline/stream/events` | server-sent events |

## The kernel

| Method | Path | What it does |
|---|---|---|
| GET | `/api/modules` |  |
| GET | `/api/modules/{name}` |  |
| PUT | `/api/modules/{name}` |  |
| GET | `/api/ui` |  |
| GET | `/space/modules/{name}/ui/{file}` | A module's custom widget (``modules/<name>/ui/<file>``), the one |
| GET | `/space/data/session_prompts.json` | Return user prompts for one session, grouped into human turns. |
| POST | `/space/server/restart` | Restart through the install's supervisor; never start a second server. |
| GET | `/space/server/status` | Lightweight status for the Space UI widget (also see /health). |
| POST | `/space/server/stop` | Gracefully stop the server. Local, same-origin only; restart via ./cowork-api.sh start. |
| GET | `/space/setup/status` | Workspace metadata and verified account status, without credential values. |
| POST | `/space/update/apply` | Fast-forward the checkout to the remote branch. Local, same-origin only, |
| GET | `/space/update/status` | Version check for the Setup tab: how far HEAD is behind the remote. |
