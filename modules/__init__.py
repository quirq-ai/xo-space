"""The Space's modules: one folder each, discovered by ``services/modules.py``.

A folder here is a module when it holds a ``module.json``. See
``services/schema/module.schema.json`` for the manifest and DEVELOPING.md
section 2 for the contract files.
"""
